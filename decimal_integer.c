#include<stdio.h>

/**
 * Format specifier to print integer values
 * @printf prints the integer values
 *
 * Returns 0
 */

int main(void)
{
	int s = 123;
	int x = 45;
	int j = 2;
	printf("s is %d and x is %d and j is %d\n",s,x,j);
	{
		return (0);
	}
}
